@if ExtraData // always false

// we need to set fake style to make sure those objects won't be optimized during generation

line|z11-[highway][surface] {
    |z13-{
        width: 1px;
        color: black;
    }
}

area|z9-{
    [natural],
    [water][salt],
    [wetland],
    [landuse] {
        [leaf_type] {
            fill-color: green;
        }
    }
}

@endif
